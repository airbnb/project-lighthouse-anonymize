name: Tests

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["main"]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.9", "3.10", "3.11", "3.12"]
        os: [ubuntu-latest, macos-latest]

    runs-on: ${{ matrix.os }}

    permissions:
      contents: read
      checks: write
      pull-requests: write

    steps:
    - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4.3.1
      with:
        fetch-depth: 0  # For hatch-vcs

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065  # v5.6.0
      with:
        python-version: ${{ matrix.python-version }}
        cache: 'pip'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install hatch hatch-vcs
        pip install -e ".[dev]"

    - name: Run tests
      run: |
        pytest -v --cov=project_lighthouse_anonymize --cov-report=xml --junit-xml=test-results.xml

    # TODO: Uncomment when ready for public release
    # - name: Publish test results
    #   uses: EnricoMi/publish-unit-test-result-action@283dea176069279a9076e77b548668a8e4f0c31b  # v2.9.0
    #   if: ${{ !cancelled() }}
    #   with:
    #     files: test-results.xml

    # TODO: Uncomment when ready for public release
    # - name: Upload coverage
    #   if: matrix.python-version == '3.12' && matrix.os == 'ubuntu-latest'
    #   uses: codecov/codecov-action@0561704f0f02c16a585d4c7555e57fa2e44cf909  # v5.5.2
    #   with:
    #     files: ./coverage.xml
    #     token: ${{ secrets.CODECOV_TOKEN }}
    #     fail_ci_if_error: true

  lint:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4.3.1
      with:
        fetch-depth: 0  # For hatch-vcs

    - name: Set up Python 3.12
      uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065  # v5.6.0
      with:
        python-version: "3.12"
        cache: 'pip'

    - name: Install dependencies
      run: |
        pip install ruff pyright hatch hatch-vcs
        pip install -e .

    - name: Check formatting
      run: ruff format --check src/ tests/

    - name: Run ruff
      run: ruff check src/ tests/

    - name: Run pyright
      run: pyright src/

  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4.3.1
      with:
        fetch-depth: 0  # For hatch-vcs

    - name: Set up Python 3.12
      uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065  # v5.6.0
      with:
        python-version: "3.12"
        cache: 'pip'

    - name: Install build dependencies
      run: pip install build hatch hatch-vcs

    - name: Build package
      run: python -m build

    - name: Verify package installation
      run: |
        pip install dist/*.whl
        pip check

  docs:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4.3.1
      with:
        fetch-depth: 0

    - name: Set up Python 3.12
      uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065  # v5.6.0
      with:
        python-version: "3.12"
        cache: 'pip'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install hatch hatch-vcs
        pip install -e ".[docs]"

    - name: Build docs
      run: sphinx-build -b html docs docs/_build/html -W --keep-going
